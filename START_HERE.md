# Obsidian AI 업무 위키 템플릿

이 템플릿은 Claude Code, Codex 같은 AI 에이전트가 업무 맥락을 저장하고 다시 참조할 수 있도록 만든 빈 Obsidian vault입니다.

이 템플릿에는 예시 고객, 예시 회의록, 예시 프로젝트 같은 더미 데이터가 없습니다. 실제 업무 자료는 사용자가 별도로 추가해야 합니다.

## 시작 방법

1. Obsidian에서 이 폴더를 vault로 엽니다.
2. Claude Code 또는 Codex를 이 폴더에서 실행합니다.
3. `prompts/first-setup.md`의 프롬프트를 에이전트에게 붙여넣습니다.
   프롬프트를 여기에 복제하지 않습니다 — 두 곳에 적으면 한쪽만 고쳐져 어긋납니다.

## 기본 명령

- `save`: 현재 작업 맥락을 저장합니다.
- `ingest`: raw 자료를 읽고 wiki 자료로 정리합니다.
- `query`: wiki와 log를 참조해 이전 맥락을 복원합니다.
- `lint`: vault 구조와 규칙 위반을 점검합니다.

## 개발 작업을 할 때

이 vault에는 개발용 서브에이전트가 함께 들어 있습니다(`.claude/agents/`). 코드 작성, 페이지 구현, 버그 수정 같은 개발 요청은 `pm` 에이전트가 먼저 받아 나머지에게 분배합니다.

위 네 개 명령은 지식 관리용이고, pm을 거치지 않습니다.

개발 규칙은 `.claude/rules/dev-common.md`에 있습니다. 설계 문서를 먼저 완성한 뒤 구현한다는 것이 가장 중요한 규칙입니다.

이 체계를 다른 프로젝트에도 그대로 옮기려면 `prompts/bootstrap-dev-agents.md`를 사용합니다.

